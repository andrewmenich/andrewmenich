{% set image = block.image.one() %}

{% if image %}
  {% set optimizedImages = image.optimizedImage %}
  <img 
    src="{{ optimizedImages.src() }}"
    srcset="{{ optimizedImages.srcset() }}"
    sizes="100vw"
    class="w-full rounded lazy"
    alt="{{image.title}}"
  />
{% endif %}