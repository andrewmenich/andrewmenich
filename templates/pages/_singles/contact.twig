{% extends "_layout" %}

{% import "_macros/heros.twig" as heros %}

{% block content %}

{{ sprig.script }}

	{{ heros.singleHero('Contact') }}

<section class="text-gray-600 ">
  <div class="container px-5 py-24 mx-auto">
    <div class="flex flex-col w-full mb-12 text-center">
      <h2 class="mb-4 text-2xl font-medium text-gray-900 sm:text-3xl title-font">Get in Touch</h2>
      <p class="mx-auto text-base lg:w-2/3">Don't be shy; I would love to hear from you!</p>
    </div>
    <div class="mx-auto lg:w-1/2 md:w-2/3">
      {{sprig('Contact')}}
    </div>
  </div>
</section>

{% endblock %}